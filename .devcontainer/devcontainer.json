{
  "name": "slide-repo",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "forwardPorts": [3030],
  "remoteUser": "node",
  "postCreateCommand": "corepack enable && pnpm -v && pnpm install && pnpm exec playwright install chromium",
  "customizations": {
    "vscode": {
      "extensions": [
        "Vue.volar",
        "bierner.markdown-mermaid"
      ]
    }
  }
}
